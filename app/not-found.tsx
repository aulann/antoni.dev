import { GhostNotFoundClient } from '@/components/ui/ghost-404-client';

export const metadata = {
  title: '404 — Nie znaleziono',
};

export default function NotFound() {
  return <GhostNotFoundClient />;
}
